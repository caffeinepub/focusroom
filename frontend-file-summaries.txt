{
  "frontend/src/App.tsx": "TanStack Router setup with root route using AppLayout; default `/` now redirects to `/login` instead of `/lobby` so unauthenticated users land on the login page.",
  "frontend/src/components/layout/AppLayout.tsx": "App shell that now properly handles the `'initializing'` auth status by showing a full-screen loader instead of rendering the Outlet, redirects unauthenticated users to `/login` after init completes, redirects authenticated users away from `/login` to `/lobby`, and shows `ConnectionErrorModal` on timeout.",
  "frontend/src/components/auth/ProfileSetupModal.tsx": "Modal dialog that blocks interaction until a new user sets their username on first login; now passes the full UserProfile shape (including xp: BigInt(0)) to saveCallerUserProfile, and updated button text to \"Enter Studyroom\".",
  "frontend/src/hooks/useGetCallerUserProfile.ts": "React Query hook to fetch the authenticated user's profile with proper actor-dependency handling to prevent modal flash.",
  "frontend/src/hooks/useSaveCallerUserProfile.ts": "React Query mutation hook to persist a user profile via the backend and invalidate the profile cache on success.",
  "frontend/src/hooks/useQueries.ts": "Streamlined React Query hooks file with room creation/joining, storeEvent (phase param removed), getRoomParticipants, and new useAwardXp mutation; all timer/session polling hooks removed.",
  "frontend/src/pages/LoginPage.tsx": "Login page that now tracks the full connecting state (logging-in + post-login actor setup), shows a `ConnectionErrorModal` on timeout, navigates to `/lobby` once authenticated and actor is ready, and displays a sign-in error message on failure.",
  "frontend/src/pages/LobbyPage.tsx": "Lobby page with create/join room functionality; removed the indefinite \"Connecting…\" banner and replaced it with a contextual \"Setting up your session…\" indicator that only shows while `actorFetching` is true.",
  "frontend/src/pages/RoomPage.tsx": "Room page using `useParams({ from: '/room/$code' })` to correctly read the `code` param, with local camera shown separately from the VideoGrid to prevent duplicate rendering.",
  "frontend/src/components/room/TimerDisplay.tsx": "Compact elapsed-time counter with clock icon, teal accent color, and monospace tabular font for stable digit width.",
  "frontend/src/components/room/CameraPreview.tsx": "Local camera preview that calls getUserMedia synchronously at the start of the click handler (before any state updates) to preserve the browser's user-gesture context and trigger the native permission dialog; handles idle/requesting/active/denied/error states with instructional messages.",
  "frontend/src/components/room/ChatPanel.tsx": "Chat panel with gradient message bubbles for local messages, styled system messages, focus-glow input field, and teal send button.",
  "frontend/src/components/room/GoalEntryModal.tsx": "Pre-session modal prompting the user to enter a study goal before each Focus session starts, with skip option.",
  "frontend/src/components/room/GoalDisplay.tsx": "Small card showing the user's current session goal during the Focus phase.",
  "frontend/src/components/room/GoalCompletionPrompt.tsx": "End-of-session modal asking the user to mark their goal as Completed or Not Yet, triggering XP awards.",
  "frontend/src/components/room/ParticipantList.tsx": "Participant list with avatar initials, current-user highlight in teal, camera-active indicator, and animated presence dots.",
  "frontend/src/components/room/LeaderboardPanel.tsx": "XP leaderboard with medal emoji ranks, per-rank glow effects for top 3, current-user teal highlight, and monospace XP badges.",
  "frontend/src/index.css": "Global CSS with rich OKLCH dark-mode design tokens, layered gradient backgrounds, glow effects, glass morphism utilities, smooth transitions, and polished scrollbar styling.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Inter/JetBrains Mono fonts, teal/amber color scales, gradient background utilities, glow shadow tokens, and animation keyframes for the Studyroom dark theme.",
  "frontend/src/camera/useCamera.ts": "Full implementation of the useCamera hook using the browser MediaDevices API, providing startCamera, stopCamera, switchCamera, capturePhoto, retry, videoRef, canvasRef, and state flags (isActive, isLoading, isSupported, error, currentFacingMode).",
  "frontend/src/components/room/AmbientSoundToggle.tsx": "New component providing an optional ambient sound toggle (rain, library, white noise, or off) with a dropdown UI; plays the selected audio at 30% volume in a loop only during Focus phase; persists selection to localStorage under `focusroom_ambient_sound`.",
  "frontend/src/components/room/DistractionLockIndicator.tsx": "New component that uses the Page Visibility API to detect tab-switching during Focus phase; shows a pulsing yellow warning dot after 10 seconds away; disappears immediately on return; inactive during Break/Idle phases.",
  "frontend/src/hooks/useWebRTC.ts": "WebRTC hook with deduplication logic — addOrUpdateRemoteStream replaces existing entries by peerId, createPeerConnection closes any existing connection before creating a new one, and signal processing skips self-signals and uses a processed-signal cache to prevent duplicates.",
  "frontend/src/components/room/VideoGrid.tsx": "Responsive video grid that deduplicates remote streams by peerId using useMemo, renders the local stream tile only when explicitly passed (null by default from RoomPage to avoid duplication), and shows empty tiles for participants without active streams.",
  "frontend/src/hooks/useGratitudeMessages.ts": "Custom hook that manages a shuffled cycle of seven predefined gratitude messages, persisting the queue and current index to localStorage using Fisher-Yates shuffle so the sequence survives page reloads and never repeats until all messages are shown.",
  "frontend/src/components/room/ScratchCardOverlay.tsx": "Full-screen modal overlay with a canvas-based scratch-off animation; renders a gold metallic gradient over the hidden message text, tracks scratched pixel percentage, and auto-reveals the message at 55% threshold with mouse and touch support.",
  "frontend/src/components/room/GratitudeCard.tsx": "Non-blocking fixed-position golden card icon in the bottom-right corner that appears after a focus session; clicking it opens the ScratchCardOverlay with the current session's gratitude message, and dismisses itself after the overlay is closed.",
  "frontend/index.html": "HTML entry point with updated title tag reading \"Studyroom\".",
  "frontend/src/hooks/useConnectionTimeout.ts": "Custom hook that monitors a boolean `isConnecting` flag and fires a 15-second timeout, returning `hasTimedOut` and `clearConnectionTimeout` to reset the state.",
  "frontend/src/components/auth/ConnectionErrorModal.tsx": "Modal dialog shown when a connection timeout occurs, offering \"Try Again\" (reload) and \"Return to Login\" (logout) recovery actions styled consistently with the rest of the app."
}