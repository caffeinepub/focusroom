{
  "frontend/src/App.tsx": "Main app entry with TanStack Router setup, defining routes for login, lobby, and room pages, wrapped in ThemeProvider (forced dark) and Toaster.",
  "frontend/src/components/layout/AppLayout.tsx": "Persistent dark-themed shell with header (FocusRoom logo, login/logout button, username display), footer with attribution, and ProfileSetupModal trigger.",
  "frontend/src/components/auth/ProfileSetupModal.tsx": "Modal dialog that blocks interaction until a new user sets their username on first login, using saveCallerUserProfile mutation.",
  "frontend/src/hooks/useGetCallerUserProfile.ts": "React Query hook to fetch the authenticated user's profile with proper actor-dependency handling to prevent modal flash.",
  "frontend/src/hooks/useSaveCallerUserProfile.ts": "React Query mutation hook to persist a user profile via the backend and invalidate the profile cache on success.",
  "frontend/src/hooks/useQueries.ts": "Central React Query hooks file covering createRoom (now also calls joinRoom and persists to localStorage after creation), joinRoom (now persists code to localStorage), useTimerState (polling every 2s), startSession, and storeEvent mutations.",
  "frontend/src/pages/LoginPage.tsx": "Full-screen login page with Internet Identity button, feature list, and auto-redirect to lobby when already authenticated.",
  "frontend/src/pages/LobbyPage.tsx": "Lobby page with create and join room flows; create room now navigates directly to the room on success (no intermediate code display step), with inline error messages for both create and join failures using AlertCircle; instant rejoin logic preserved.",
  "frontend/src/pages/RoomPage.tsx": "Main room page updated to persist room code to localStorage on join (instant rejoin), remove host-only restrictions on session start, pass `totalSeconds` to TimerDisplay, and integrate AmbientSoundToggle (in header) and DistractionLockIndicator (fixed overlay).",
  "frontend/src/components/room/TimerDisplay.tsx": "Enhanced timer display with an SVG circular progress ring that depletes clockwise as the countdown progresses; adds a soft radial glow animation for ~1.6s when a Focus session begins; accepts a new `totalSeconds` prop for ring progress calculation.",
  "frontend/src/components/room/CameraPreview.tsx": "Camera preview component where `getUserMedia` is called as the absolute first async operation in the click handler (before any state updates) to ensure the browser's native permission popup is triggered immediately; applies a permanent inline CSS blur filter to the video element at all times, shows clear error messages on denial, and stops all tracks on unmount.",
  "frontend/src/components/room/ChatPanel.tsx": "Chat panel with message list and input; applies strong CSS blur (blur(8px) !important) to all messages during Focus phase, revealing them during Break.",
  "frontend/src/components/room/GoalEntryModal.tsx": "Pre-session modal prompting the user to enter a study goal before each Focus session starts, with skip option.",
  "frontend/src/components/room/GoalDisplay.tsx": "Small card showing the user's current session goal during the Focus phase.",
  "frontend/src/components/room/GoalCompletionPrompt.tsx": "End-of-session modal asking the user to mark their goal as Completed or Not Yet, triggering XP awards.",
  "frontend/src/components/room/ParticipantList.tsx": "Participant list panel showing the current user (and any locally known participants) with presence indicators.",
  "frontend/src/components/room/LeaderboardPanel.tsx": "In-room XP leaderboard sorted descending, showing medal icons for top 3, highlighting the current user's row.",
  "frontend/src/index.css": "Global CSS extended with keyframe animations for the distraction-dot pulse (`distraction-pulse`) and timer glow fade (`timer-glow-fade`), plus component classes `.distraction-dot` and `.timer-glow`.",
  "frontend/tailwind.config.js": "Extended Tailwind config with JetBrains Mono/Inter fonts, FocusRoom design tokens (room-bg, room-surface, room-input, room-border, room-text, room-muted, focus-accent amber, break-accent sage), and all shadcn color mappings.",
  "frontend/src/camera/useCamera.ts": "Full implementation of the useCamera hook using the browser MediaDevices API, providing startCamera, stopCamera, switchCamera, capturePhoto, retry, videoRef, canvasRef, and state flags (isActive, isLoading, isSupported, error, currentFacingMode).",
  "frontend/src/components/room/AmbientSoundToggle.tsx": "New component providing an optional ambient sound toggle (rain, library, white noise, or off) with a dropdown UI; plays the selected audio at 30% volume in a loop only during Focus phase; persists selection to localStorage under `focusroom_ambient_sound`.",
  "frontend/src/components/room/DistractionLockIndicator.tsx": "New component that uses the Page Visibility API to detect tab-switching during Focus phase; shows a pulsing yellow warning dot after 10 seconds away; disappears immediately on return; inactive during Break/Idle phases."
}